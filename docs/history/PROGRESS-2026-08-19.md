# PROGRESS — XPR Music / Onda (2026-08-19 → 20)

**Track:** streaming paywall + testnet contract + login gate  
**Seat:** Grok Build (implement; Gabriel directed, no sub)  
**Live:** https://music.project-testing.xyz · Hetzner `167.233.60.62` `/opt/xpr-music`  
**Protocol:** this file = durable record; `NOW.md` = live status

---

## State (true now)

| Surface | True |
|---|---|
| Public site | **Up.** Caddy → `:8788`. Unit `xpr-music` active. |
| Play | **Wallet required.** Guest `POST /api/session/play` → `401 wallet login required to play`. Browse/library still open. |
| Payments | **`payments_enabled: false`.** Simulated 2s hold in Node. No chain transfers from the app. |
| Meter | `app/stream-meter.js` — 2s lock, vest play, rebate on pause/skip/end; stale >3s vests the open window only. |
| Contract | **Testnet `ondastream`** deployed. Same shape as Metal X `vest`, 2s window + song registry. **Not wired to the app.** |
| NFTs | **Not minted.** Talk concluded: NFT ≠ cash register. Blueprint only. |
| Login | Identity-proof path (WebAuth / Anchor / Proton). Mainnet login in maintenance. CLI keys ≠ Face ID. |

**Auth E2E (2026-08-20):** `BASE_URL=https://music.project-testing.xyz node test/verify-auth.mjs musictesting` → **11/11 PASS** (guest 401, logged-in play Signal Bloom, replay reject, scoped library). Same 11/11 on local `:8788`.

---

## What we did (in order)

### 1. Test wallet (not `music.testing`)

XPR names: 1–12 chars, `a-z` + `1-5`, **no dots**. Created **`musictesting`** on testnet (20 XPR, persist-first, `felixpaw` funded). Keys `~/.xpr-testnet/musictesting.key.json` (0600). Dapp account `xprmusic` already existed.

Proton CLI keychain was **locked** (`key:list` hangs). Do not retry.

### 2. 5s vs 15s vs what actually ran

Live tick was already **per-second USD** (`USD_PER_SEC = 0.00005`), not 5s or 15s buckets.

- **0.5s** = Metal X Stream help-doc drip (block time)  
- **5s** = old `claimvest` hypothesis / network poll  
- **15–30s** = original **lock window** (crash bound), not the meter  

Parent: https://app.metalx.com/stream → mainnet `vest`. Ground truth: `felixpaw` `startvest` 10 XPR / ~24h to `felixpawbot` (tx `e05c8745…`, 2026-08-09).

### 3. 2s hold + rebate (same vest, faster)

Gabriel: sub-5s, then **2s hold, pay back unused where play stops.**

Shipped in `stream-meter.js`, wired into `server.js`. Pause/skip/end rebate leftover. Crash (no heartbeat >3s) pays **only the open 2s** (~$0.00010). CC tracks free.

Tests: `cd app && npm run test:meter` (10/10). HTTP: 0.5s pause billed $0.000025 + rebate.

### 4. Live on Hetzner (not Vercel)

Rsync **`server.js` + `stream-meter.js` only** (later also HTML for the gate). **Never rsync `catalog/`** (auth-secret, used-proofs, live library).

Rollback: `/opt/xpr-music/server.js.bak-pre-2shold-20260819T225912Z`

### 5. Custom contract `ondastream` (testnet)

Gabriel: custom one, do it ourselves. **Not** a fork of Metal X UI; **not** mainnet.

- Account `ondastream` · explorer testnet · wasm `39719c37…` (claim-walker build)  
- Source: `~/Developer/xpr-music/contracts/ondastream/`  
- `setsong` registry (payee ≠ typed `to`)  
- Memo `s:<song_id>` starts/tops the 2s vest  
- `stopstream` rebate · `claim` artist withdraw  

Smoke: `musictesting` sent **0.0002 XPR** `s:signal-bloom` → stop → leftover back, **0.0001 XPR** to `xprmusic` then claimed.

Traps paid: notify cannot bill listener RAM (payer = contract); `Singleton.get()` is never null (use `getOrNull`); `Symbol.raw()` must not key `claim`.

### 6. NFT talk (no mint)

**Streaming does not need an NFT.** Payee is `ondastream.songs.payout`.

NFT **cannot hold** the stream contract (it’s AtomicAssets data). Combined only as: *this NFT is that paying song* (`song_id` join). Holder does **not** get listen money.

Blueprint (not built): `BLUEPRINT-nft-ondastream.md`. Collection `xprmusic` not created on AA testnet yet.

### 7. Login gate restored (2026-08-20)

Playback was opened 2026-08-17 for testers. Re-gated per `BLUEPRINT.md` four-point list:

1. `server.js` `/api/session/play` → 401 without Bearer  
2. `playback_requires_wallet: true`  
3. Desktop `requireWalletPlayback()` toast + pill nudge  
4. Mobile WebAuth guard on `playSong(autoplay)`  

Deployed to Hetzner `web/desktop.html` + `web/mobile.html`. Verified live E2E.

---

## What is NOT done

1. **App → `ondastream`.** Play still uses simulated meter, not `transfer` + `stopstream`.  
2. **`payments_enabled`.** Still false. Invert SHIP before money-on.  
3. **Human WebAuth login.** Gate works; you still need a **testnet.webauth.com / Anchor** account. CLI `musictesting` is not Face ID.  
4. **NFT mint.** Parked unless we want a collectible.  
5. **Mainnet `ondastream`.** Forbidden until SHIP.  
6. **`xprmusic` RAM/newaccres** if we later createcol on AtomicAssets (account is resource-poor).

---

## Next (first safe actions)

1. Import or create a **testnet WebAuth** account and press the live login pill (proves the human path the E2E already proved with CLI keys).  
2. Wire play/pause to `ondastream` **on testnet only** (memo `s:<id>`, `stopstream`). Keep `payments_enabled` false until that smoke is boring.  
3. NFT only if Gabriel wants a wallet object — not required for (2).

---

## Paths / ops (do not rediscover)

| How | Where |
|---|---|
| Live URL | https://music.project-testing.xyz |
| Deploy app | rsync `server.js` `stream-meter.js` `web/{desktop,mobile}.html` → `/opt/xpr-music/` (+ `web/`). `systemctl restart xpr-music`. Never `catalog/`. |
| Contract | `contracts/ondastream/README.md` |
| NFT join | `BLUEPRINT-nft-ondastream.md` |
| Tick vs vest | `~/knowledge/tech-stack/xpr-network/onda-streaming-tick-vs-vest-2026-08-19.md` |
| Verify login+gate | `cd app && BASE_URL=https://music.project-testing.xyz node test/verify-auth.mjs musictesting` |
| Verify meter | `cd app && npm run test:meter` |

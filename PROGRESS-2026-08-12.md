# PROGRESS — XPR Music (2026-08-12 / 13)

**Later (paywall / contract / login gate):** `PROGRESS-2026-08-19.md`.

**Track:** product UI (desktop + mobile) · **Seats:** Grok Build (orchestrate), Luna (earlier desktop/mobile), M3 (mobile thrash)  
**Protocol:** research-checkpoints (this file = durable record; NOW.md = live status)

## State (true now)

- **Desktop live** `app/web/desktop.html` — play bar: single row (no empty dome strip), **lean metrics** (rates + session totals only), volume + transport + seek. Spinning Onda logo (Grok) kept. Payment clutter (DEMO RATES / simulated copy / coin pills) removed.
- **Mobile live** `app/web/mobile.html` = Luna-attempt keep (API + mock-shaped NP). **Visual truth** = bench `gpt-5.6-luna-mobile.html` (not live).
- **Hermes default** = MiniMax-M3 (not Grok, not Kimi) — Gabriel “sorry m3”.
- **Thrash cleaned** → `app/web/_archive/session-thrash-2026-08-13/`. Restore plan A = `app/web/.versions/`.
- **Server** `:8788` when up.

## Round 2 — bar fixes + create-playlist bug (2026-08-13, claude-code)

Gabriel reported 4 issues. All fixed in `app/web/desktop.html`, verified in a real
browser (Playwright + full Chromium), md5 `7db1e52e…` → `df3838fc…`.

1. **License line restored** under the artist name in the bar. Grok had set
   `.player .now-license { display: none }` as an unrequested declutter; Gabriel wants
   it visible. Now `display: block`, 9.5px, `var(--dim)`, ellipsis.
2. **Icon spacing** — `.now-tools` `margin-left: 2px` → `16px` (heart/queue no longer
   crowd the artist text; measured 28px gap from the text block).
3. **Dome/expand button moved.** Was `left: 50%` + `translate(-50%, -100%)` — centered,
   floating fully above the bar. Now a direct child of `.player-row` (not `.player-center`),
   `right: 28px`, `transform: translateY(-55%)` → sits on the **right**, **overlapping the
   bar's top edge by 15px** instead of floating above it. Absolutely-positioned, so it does
   not disturb the 3-column grid. Still opens Now Playing (verified).
4. **"Create playlist" was NOT broken — the error feedback was invisible.** Real cause:
   on the failure path (most often HTTP 409 duplicate name) the dialog stays open, and
   `dialog.showModal()` puts it in the browser **top layer**, so the global `toast()`
   rendered *under* the `::backdrop` — invisible. Fixed with an inline
   `#createPlaylistError` inside the dialog + refocus/select of the name field.
   Full write-up: `~/knowledge/debug/dialog-top-layer-swallows-toast-2026-08-13.md`.

5. **Bar given more mass (1.3x).** `--player-body` 80px → **104px** (Gabriel: "too thin").
   Height alone left the 52px art floating in 27px of dead space, so `.now-art` was scaled
   the same 1.3x → **68px** (radius 10→12), giving even 19/18px gaps. Everything else
   re-centres itself (`align-items: center`); dome overlap and inset unchanged (15px / 28px).

6. **Bar contents scaled 1.25x** (Gabriel, after the 1.3x height): art 68→**85px** (radius
   12→14), icon buttons 32→40 (svg 17→21), transport 34→42 (svg 17→21), play 40→**50**
   (svg 18→22), title 13.5→17, artist 11.5→14.5, license 9.5→12, `.now-copy` max-width
   220→275 (license no longer truncates), progress 248→310px / 10→12.5px, seek track 4→5
   + thumb 11→14, volume + payment pills 34→**42px** tall (font 11.5→14.5, dot 7→9,
   slider 72→90px), repeat badge 12→15. Verified no collisions at 1440/1280/1100px,
   no horizontal overflow, dome still clears the payment pill.

7. **Dome position settled (Gabriel picked from rendered variants).** Two blind guesses
   missed, so four positions were rendered against the live bar and shown for a pick:
   **C = flush** — dome lays ON the bar instead of floating above it. Implemented as
   `--dome-rest: translateY(-10%)` (a var so the resting and `:active` transforms cannot
   desync). Pure `translateY(0%)` was 2px into the XPR pill's top border; -10% clears it
   by 2px while still reading flush. Verified no collision at 1440/1280/1100px.
   **Lesson:** for a pure-taste geometry call, render the options and let Gabriel pick —
   cheaper than another guess-and-miss round trip.

7b. **…then settled properly: the button now lives INSIDE the bar.** Flush still wasn't it
   ("still no", 4th miss). Asked with interpretations rather than guessing again; answer was
   **"fully inside the bar — nothing crosses the top edge."** So it stopped being an edge
   dome entirely: the element moved out of absolute positioning into `.player-right` as a
   normal flex item (58×42px gold pill, radius 999px, after the XPR pill). Removed with it:
   `--player-dome` + `--dome-rest` vars, the dome-shaped `border-radius: 44px 44px 0 0`,
   the upward `0 -4px 18px` glow (now `0 2px 12px`), and the two `::before/::after` grip
   hairlines that only made sense on a dome peeking over an edge. Collision-free by
   construction — it's in the flow, not overlaid.

8. **Sliders lengthened + volume unbubbled** (Gabriel): time slider 310→**440px**, volume
   90→**130px**, and `.volume` lost its pill (border/background/padding → none, hover now
   only brightens the icon). Known side effect: the wider centre column re-truncates the
   license line; drop the time slider to ~400px to get it back.

9. **Slide-open button REMOVED entirely** (Gabriel: "just remove the button"). After the
   inside-the-bar version crowded the right cluster, the dome was briefly restored with a
   reserved 126px column — then cut altogether. Removed: the `<button>`, its ~95-line CSS
   block (dome shape, glow, grip hairlines, pulse + `@keyframes expandPulse`,
   `body.now-playing-open` hide rule), the `.player-right` reservation padding, the
   `prefers-reduced-motion` entry, and the stale `overflow: visible` comment.
   **Critical:** the `$("playerExpand").addEventListener` had to go with it — a live
   `$()` on a deleted id returns null and would have thrown, killing the whole inline
   script. Verified 0 refs remain, `node --check` PASS, no runtime JS errors.
   **Now Playing is still reachable** — `dblclick` on the bar (verified opening it), plus
   `?nowplaying=1`. The dblclick guard's `.player-expand` selector was dropped too.
   Bar is now: art + copy + tools · transport + seek · volume + XPR pill.

**Process note:** `dispatch-grunt.sh --from claude-code` writes to
`~/.pi/coms/claude-code-outbox/`, which **no bridge drains** — the job silently never
reached Vulcan, and the log tail showed the *previous* job's DONE, which read as success.
Claude Code must dispatch via **direct Unix socket** (recipe in `~/CLAUDE.md`), or that
script needs a `claude-code` route. Gabriel then reassigned this work to claude-code
directly ("do it yourself"), overriding the standing grunt-offload HARD STOP for this task.

10. **Now Playing: vertical volume + like/add** (2026-08-13, claude-code).
   - **Vertical volume on the sheet's right edge** — `+` on top, `−` below, live % readout.
     **Top = louder** (verified: click top → 1.00, click bottom → 0.00).
   - **Sync is real, both directions**, via a single `setVolume()` source of truth:
     sheet ↔ bar ↔ `audio.volume` all verified equal after +/− clicks and after driving
     the bar slider. `paintVolume()` skips writing to the sheet input while it has focus
     so it can't fight the user mid-drag.
   - **TRAP:** do **not** use `appearance: slider-vertical` for the vertical range —
     Chromium then renders the *native* control and silently discards
     `::-webkit-slider-thumb` styling (it shipped as a blue OS slider). Use
     `appearance:none` + `writing-mode: vertical-lr` + `direction: rtl` (rtl is what puts
     max at the top). Verified via computed style + gradient presence, not by eyeballing.
   - **Like + Add buttons** added to the sheet (`#sheetLike`, `#sheetAdd`).
     `updateLike()` now drives the bar heart and the sheet heart from one call, so they
     can never disagree. Add reuses `openAddToPlaylist(currentSong.id)` — falls back to
     Create-playlist when the user has none, same as the song cards.
   - `.sheet-actions` is **absolutely** positioned: as a flex sibling it pushed the centred
     title 54px off-centre against the album art. Now title centre === art centre === 720.
   - Hidden under 1100px wide.
   - **Still open from Gabriel's message:** "and the border down" was not actioned —
     meaning unclear; and whether `mobile.html` should get the same volume control
     (mobile currently has **no** volume UI at all).

11. **All six rows converted to sliding rails + < > paging arrows** (Gabriel 2026-08-13).
   `card-grid` (Music Originals, Newly Minted, Favorites, Playlist) was a wrapping
   multi-row CSS grid — now one flex row that scrolls, with `flex-basis` reproducing the
   old grid-column math so it's still **exactly 5 visible** at desktop width (4 at
   ≤1250px, 3 at ≤960px), verified programmatically, not eyeballed. `h-rail` (Recently
   Played, Artists) unchanged in card size. Every row got a `.rail-nav-group` (reused
   the existing `.queue-nav` button style) wired to `rail.scrollBy()`; arrows
   disable/hide themselves when the row doesn't overflow (e.g. Favorites with 2 songs)
   — `syncRailFade` now drives both the edge-fade AND the arrow disabled/hidden state
   from one scroll-geometry read. Native scrollbar fully hidden on every rail
   (`scrollbar-width: none` + `::-webkit-scrollbar{display:none}`) — was a visible thin
   strip during trackpad swipe that read as a rendering bug.
   Arrow spacing was too tight on the first pass ("too close lol") — gap 6→16px,
   +6px inset from the section edge.
   `RECENT_MAX` 18/24 (two different caps, inconsistent) → one `RECENT_MAX = 40` constant.

12. **Real bug found + fixed: no queue scoping existed at all.** Every row shared ONE
   global queue (`state.songs`, storage order) — playing from Music Originals (3 songs)
   and hitting Next walked into unrelated catalog tracks, and "Up Next" always listed
   the entire catalog regardless of what you'd clicked. Root cause: `nextIndex()` and
   `queueOrderIndices()` both did `(state.currentIndex + i) % state.songs.length` with
   no concept of "which subset am I actually playing." Fixed with `state.queue` (array
   of global indices = the active context) + `setQueue()` / `playFromRow()` /
   `activeQueue()`; `card()` now takes a `rowSongs` 4th arg so every render call
   (`renderGrid`, `renderRecent`) queues exactly what's rendered — Music Originals stays
   Music Originals, Browse stays the filtered results, Favorites/Playlist stay their own
   set, Recently Played stays recency-ordered. Up Next queue items still jump within the
   CURRENT queue rather than resetting it (`context === "queue"` short-circuits back to
   plain `playSong`). **Onda Radio is the deliberate exception** — `state.queue = []`
   before playing, so shuffle covers the whole catalog; verified it reaches a song
   outside a previously-active Favorites scope. Shuffle inherits this for free — it now
   also stays scoped to whatever row you're in, not just Next/Prev.
   Verified live (not by reading code): Music Originals Next-spammed 7x, all 7 plays
   stayed inside the 3-song set; Up Next sheet listed only the other 2; Favorites
   Next-spammed 5x, all stayed inside Favorites and never leaked into Music Originals;
   Onda Radio broke scope and reached an outside song.

13. **Now Playing polish round** (Gabriel 2026-08-13).
   - **3-dot "More" button removed** (`#sheetMore`) — was a dead stub (`toast("...coming
     later")`). Heading stays centered: `.sheet-topbar` is a `44px 1fr 44px` CSS grid,
     so an empty 3rd column doesn't need special handling.
   - **Background redesigned** to match the app's established language instead of a flat
     glow: layered gold radial (top) + the same concentric-ring texture `.main` already
     uses (subtle, upper-right) + a second warm glow grounding the transport area, over
     the `#0b0b10` base.
   - **Payment pill → two rows.** Was a single-row 42px pill; now a column: crypto
     ticker+amount on top, `$` USD estimate underneath (`border-radius` 999→16px so a
     taller shape reads right). The live-dot moved from `.payment::before` to
     `.payment-rates::before` — it was a flex sibling of the row, would've become its
     own row once `.payment` switched to `flex-direction: column`.
   - **A real second instance now lives in Now Playing itself** (`#sheetPayment`, under
     the seek bar) — previously the ONLY payment pill was in the bar, which sits fully
     behind Now Playing's opaque full-bleed overlay, so there was literally no spend
     display visible while Now Playing was open. Bar pill and sheet pill are two DOM
     nodes sharing one `updateMeter()` — same paired-id pattern as `updateLike`/
     `updateRepeatButton`. Verified: switching currency from the sheet's pill updates
     the (hidden-behind-sheet) bar pill too.
   - **USD rates tuned for visibility, not "accuracy."** First pass used a placeholder
     rate close to real XPR/METAL/LOAN pricing — at 3 decimals every one of them rounded
     to `$0.000` for the ENTIRE session (only USDC, pegged 1:1, ever showed anything).
     Caught by testing live, not by reading the math. Bumped `usdRate` (xpr .42, metal
     .65, loan .012, usdc 1 — same placeholder status as the earlier METAL accrual rate)
     so $ moves visibly within a few seconds, matching the bar the crypto-count fix
     itself set earlier tonight.
   - **Click → cycle replaced with click → dropdown.** `#currencyPicker` is ONE shared
     panel (not duplicated per pill), `position:fixed`, JS-positioned under whichever
     pill triggered it via `getBoundingClientRect()`. Each row: dot + ticker + live
     amount + $ estimate; click selects and closes.
     **Real bug caught by testing, not spec-reading:** opening it "always downward" ran
     it 136px off the bottom of the viewport when triggered from the bar pill, which
     sits at the very bottom of the screen. Fixed with adaptive flip — measures its own
     height (layout size is unaffected by `opacity`/`transform`, only `hidden`), opens
     upward when there isn't room below, `.open-up` swaps `transform-origin` so the
     slide-in motion still emanates from the correct edge. Verified both directions:
     bar trigger → `open-up`, fully on-screen; sheet trigger → opens down, fully
     on-screen.
   - Closes on: selecting a row, outside click, Escape, resize, or ANY scroll (capture
     listener — reuses the pattern already wired for rail-fade sync).
   - **Cleanup:** `#paymentSess`/`#totX` deleted — dead scaffolding that predated the
     sheet pill existing, always `display:none` in the bar and never rendered anywhere
     else; it was the source of a duplicate-id false-positive from an earlier grep.
     `cycleCurrency()` fully removed, replaced by `selectCurrency(index)`.

14. **Round 3 of feedback** (Gabriel 2026-08-13).
   - **Real bug: current-playing card clipped at the top in every card-grid row**
     (Music Originals, Browse, Favorites, Playlist — not h-rail). Root cause:
     `overflow-x:auto` implicitly forces `overflow-y:auto` too (CSS spec), and
     `.song-card:hover`/`.current` lifts `translateY(-2px)` — `.card-grid` had
     **zero** top padding to absorb that lift, so the currently-playing card's top
     edge got sliced by the row's own scroll box. Verified with real measurement
     (`cardTop < gridTop`) before and after. Fixed: `.card-grid` padding 0→`5px 2px
     10px`; bumped `.h-rail`'s 2px (which barely worked, no real margin) to match.
   - **Settings + Artist Login** added to the sidebar under Onda Radio — no real
     screens exist yet, so both are honest placeholders (`toast("... coming soon")`),
     not fake pages. **Real bug caught before shipping:** the existing generic
     `.nav-link` click handler called `showView(button.dataset.view)` for every nav
     button; these two have no `data-view`, so `showView(undefined)` would have
     blanked the entire page. Scoped that handler to `.nav-link[data-view]` and gave
     the new two their own listeners.
   - **Lyrics centered** — `.sheet-panel` was `text-align:left`, the one block on the
     whole Now Playing screen not centered (title/artist/license were already proven
     centered via measurement, so "subtitles centered" most likely meant the lyric
     lines, not those).
   - **Time slider explained, and it was worse than first diagnosed.** Asked to
     explain, not just fix — investigated properly. The license line wasn't only
     truncating below the 1250px breakpoint I'd patched earlier; it truncated the
     instant the window was *any* narrower than a fully-maximized 1512px (needed
     273px, had exactly 273px at best — zero slack). Root cause was two earlier
     changes compounding: the 1.25x scale pass took license font 9.5→12px (net +30px
     of needed width) and the seek-bar widening (310→440px) ate the column's spare
     room — together they erased all margin. Fixed at the source: license font
     12→10.5px (243px needed, real slack now), left/right grid columns split
     asymmetrically (`minmax(255px,1fr) / minmax(190px,1fr)` — left holds text, right
     just holds a pill) instead of even 200/200, progress narrowed further at the
     breakpoint (300px). Now clean down to ~1450px; below that it still ellipses —
     flagged honestly rather than claimed fully fixed, since full coverage down to
     1100px isn't reachable without illegible text or a bigger restructure.
   - **Up Next: real bug, current song could never be highlighted.** The `.current`
     CSS existed and looked correct, but `queueOrderIndices()` deliberately excludes
     `state.currentIndex` from its own output (it only lists what's next) — so the
     highlight class was checked against a list that could never contain a match.
     Dead code that looked like a bug in the CSS but was actually a data bug.
     Fixed: `renderSheetQueue()` now prepends `state.currentIndex` as item 0.
   - **Up Next converted from fixed 5-per-page grid to a real sliding row** — same
     treatment as the other 6 rows (`display:flex`, `scroll-snap-align`, registered
     in `RAIL_ARROWS`). Removed `shiftQueuePage`/`queuePageCount`/`QUEUE_PAGE_SIZE`/
     `state.queuePage` and the empty-slot padding hack entirely — dead once the row
     just scrolls instead of re-rendering discrete pages. `queuePrev`/`queueNext`
     reused as-is (already `.queue-nav` styled) via the shared `shiftRail()`.
   - **Background, round 2:** Gabriel liked round 1's direction but wanted it more
     distinct from `.main` and less dark. Blob opacities bumped ~35%
     (.30/.22/.26/.16 → .40/.30/.35/.22) AND their falloff tightened (60ish%→~50%)
     so they read as actual shapes, not a wash. Base lightened `#0b0b10` flat →
     `#1c1720…#0d0b10` gradient.
   - **New: colored backlight behind the album art**, using the same `--card-color`
     already set app-wide per song. First attempt was nearly invisible — **found via
     pixel-level testing on a contrasting-color track (blue), not by eyeballing the
     gold demo track**, that the glow's own strongest, most-saturated center sat
     directly UNDER the opaque art image, so only the gradient's already-fading tail
     ever peeked past the art's edges. Fixed by keeping the gradient fully saturated
     out to 55% of its own radius (past where the art's edge sits) before fading,
     enlarging it (480px→620px), less blur (70→46px) so color survives instead of
     dissolving, opacity .6→.8. Verified genuinely visible on both a gold and a blue
     track.

15. **Quick follow-up round** (Gabriel 2026-08-13, right after round 3).
   - **Album art glow reverted.** Fully removed `.sheet-art-row::before` and the
     `position:relative`/`z-index` scaffolding it needed — back to plain darkness
     behind the art, as it was before round 3.
   - **Two more background blobs, centered.** The 4 existing blobs all sat near
     corners (14%/-8%, 104%/12%, 90%/96%, 4%/100%), leaving the middle column — where
     title/seek/pill/transport/lyrics/queue all actually sit — visibly darker than the
     sides. Added `50%/30%` (warm) and `50%/82%` (rose-purple), same family as the
     existing four, moderate opacity so they don't fight text legibility.
   - **Currency pill moved to the top-left**, mirroring how `.sheet-volume` already
     pins to the top-right — moved out of the scrollable `.sheet-body` to be a
     sibling of `.sheet-topbar`/`.sheet-volume` inside the fixed sheet container, so
     it stays put while lyrics/queue scroll underneath (verified: identical position
     before/after scrolling). Shrunk 220→168px and dropped the oversized 16px font
     override now that it's a compact corner element, not a centered hero pill.
     Currency-picker dropdown needed no changes — its existing adaptive up/down logic
     just naturally opens downward now that the trigger sits near the top.

16. **Real repeat bug: the loop never actually stopped, even though the counter
   correctly hit 0.** Reproduced deliberately (search narrowed to exactly 1 result,
   so the scoped queue has one song) rather than guessing: repeat badge counted
   2→1→0 correctly and then STAYED at 0 forever — but the song kept silently
   replaying past it, because `nextIndex(1)` in a length-1 queue always resolves
   back to the SAME index, making "advance" indistinguishable from "still
   repeating." This predates tonight and was always latent, just masked in earlier
   testing because that used the full 8-song catalog. Fixed in `handleTrackEnded()`:
   once repeat is exhausted, if `nextIndex(1) === state.currentIndex` (nothing else
   to advance to), call the existing `pauseSong()` instead of silently replaying —
   verified `audio.paused` flips true and the play button flips to "Play" at exactly
   the point it used to keep looping. Multi-song queues unaffected (re-verified: 2x
   repeat then correctly advances to a different song, re-arms to 2).

## What went wrong (for next agent)

1. Used **Dreamscape/Table** as if it were mid-flight continuity — wrong surface. Research already uses **PROGRESS + NOW**.
2. **M3** painted mobile gold from desktop; reverse thrash burned SuperGrok. M3 never wrote desktop.
3. No PROGRESS checkpoints during multi-hour UI → re-derived “original” from agent logs.
4. Agent-memory `record-talk` is insufficient; subconscious now harvests CHANGELOG/versions (still not a substitute for this file on long tracks).

## Next safe actions

1. Mobile planes/swipe only when Gabriel says go (plan: `PLANE-SWIPE-PLAN.md`); snapshot first.
2. If more desktop bar work: `iterate-file-version.sh` then edit; update this PROGRESS on state transition.
3. Do **not** re-theme mobile from desktop without explicit order.

## Evidence

- `.versions/desktop.html.*` (pre-playbar-polish, pre-declutter, pre-metrics-sliders, …)
- `DESIGN-SOURCE-OF-TRUTH.md` · `DESKTOP-SOURCE-OF-TRUTH.md`
- Canon continuity: skill `research-checkpoints`

17. **3 Now Playing background design mockups** (Gabriel asked for options, not more
   live iteration). Each is a FULL COPY of the working `desktop.html` (not a static
   picture) with only the `.now-playing-sheet` background swapped — so they're fully
   clickable/playable for real comparison, not just screenshots.
   - `nowplaying-bg-A-canvas.html` — ambient wash from the song's own `--card-color`
     (Spotify-Canvas-style, but color-only so it works with or without real art).
   - `nowplaying-bg-B-aurora.html` — soft diagonal color bands, slow 24s drift
     animation (`prefers-reduced-motion` respected). The only one that moves.
   - `nowplaying-bg-C-spotlight.html` — deliberately the quiet option: one soft warm
     light from above, fast falloff to near-black. No side color, no motion.
   **Real bug caught before showing these:** first pass gave the glow pseudo-elements
   `z-index: 0`, which — per CSS stacking rules — paints a POSITIONED z-index:0 layer
   ABOVE unpositioned normal-flow content regardless of DOM order, so the glow sat on
   top of the title/lyrics text and washed it out. Fixed with `z-index: -1` (paints
   above the sheet's own solid background, below all real content). Caught by
   rendering and looking, not by reasoning about the CSS.
   Live at `:8788/web/nowplaying-bg-{A-canvas,B-aurora,C-spotlight}.html?nowplaying=1`.
   **Not wired into the live `desktop.html`** — throwaway comparison copies, don't
   bother keeping them in sync if `desktop.html` changes further; delete once Gabriel
   picks a direction (or none) and it's carried into the real file.

18. **Now Playing background — decided: "Canvas" (variant A).** After 3 rendered
   directions (Canvas, Aurora, Spotlight — see #17), Gabriel chose Canvas: ambient
   backlight from `--card-color`, no fixed pattern, genuinely different per song.
   Adopted directly into `desktop.html`'s `.now-playing-sheet`, replacing the old
   6-blob mesh from earlier tonight — `::before` (blurred color wash) + `::after`
   (radial dark overlay). Two live rounds of feedback baked in before it was final:
   - "black start lower in the transition" — the dark overlay's mid-stop moved from
     50%→60% of the ellipse's own radius (and end 100%→108%), so color holds through
     most of the screen; black only really commits near the very edge now.
   - Checked contrast on the pill + lyrics box against the new background — legible
     without extra work, unlike the Aurora mockup (which needed a frosted-glass fix
     after its vibrancy pass). Canvas never got pushed that saturated.
   All 3 mockup files (`nowplaying-bg-{A-canvas,B-aurora,C-spotlight}.html`) deleted
   now that the decision carried into the real file — version snapshots exist in
   `.versions/` if any of them are ever needed again. Also found and deleted 3
   further orphaned draft copies (`nowplaying-bg-{A,B,C}-sharp.html`) — an earlier,
   superseded pass at the same "too muddy, need it sharper" fix, made before that
   exact fix was applied directly to the canvas/aurora/spotlight files Gabriel
   actually reviewed. Confirmed via their own header comments (same authorship,
   same "Gabriel 2026-08-13" style) before deleting — not someone else's work.

19. **INCIDENT: a stale grunt job silently undid a real decision, hours later.**
   The `Vulcan-Minimax-M3` job dispatched for "bar round2" early tonight (see the
   dispatch-script gap noted after item #4) was told to ABORT once claude-code took
   over the work directly. It did not actually stop — `queue_depth: 22` on the agent
   confirms it was still holding a backlog. Sometime between the repeat-loop fix
   (item #16, ~08:05) and adopting the Canvas background (item #18, ~09:11), it
   finally executed its OLD, stale HANDOFF instruction (Issue #3: reposition the
   dome/expand button) and **reintroduced `#playerExpand` — a feature Gabriel had
   explicitly asked to have removed entirely, hours earlier ("just remove the
   button", item #9)**. Version snapshots confirm the exact window: 0 references at
   `desktop.html.…151650Z…pre-repeat-single-song-loop-fix` (08:05), 12 references by
   `…161224Z…pre-canvas-bg-adopted-live` (09:11) — and a `vulcan-minimax-m3-bar-
   round2-post` snapshot the grunt wrote itself, byte-identical to mine, confirming
   it was the source. **Caught by the routine regression sweep** (`expandGone`
   check), not by watching for it — this could easily have shipped unnoticed.
   Fixed: removed the button again (same CSS/HTML/JS removal as item #9), verified
   `expandGone: true` + no collisions + everything else from the session intact.
   Sent a hard stand-down to the agent instructing it to discard anything queued
   for this file and to treat any instruction it's holding for `desktop.html` as
   stale, since the file has had ~15 rounds of live changes since that job was
   dispatched. **Lesson for next agent:** a grunt told to "abort" is not guaranteed
   to actually stop — if you dispatch, then take over work yourself, verify later
   in the session that nothing it was holding landed anyway. `queue_depth` on the
   agent registry JSON is the tell.

20. **Mobile: plane/swipe system built** (2026-08-13, claude-code) — the piece
   `PLANE-SWIPE-PLAN.md` had parked as "not built" until Gabriel said go.
   - **Top plane** (`#planeTop`, wraps art/title/progress/transport): horizontal
     swipe = prev/next song, reusing the existing `chooseNext()`/`playSong()`
     rather than duplicating logic. "Now Playing" title text removed (empty
     `.topbar-title` div kept for the 3-column grid's spacing). A drag-handle dot
     added above the art as the swipe-down affordance — same destination as the
     existing `#browseJump` button (scrolls to the queue), not a new action.
   - **Bottom plane** (`#planeBottom`, 3-pane carousel): lyrics | meter (default) |
     artist, swipe left→artist, swipe right→lyrics, plus a 3-dot indicator between
     the transport row and the meter (clickable too). Lyrics/artist content didn't
     exist in mobile at all before this — ported the same `DEMO_LYRICS` fallback +
     stub artist-stats pattern desktop's Now Playing sheet already uses.
     **Deliberate deviation from the plan:** the "Up next" queue strip stays
     OUTSIDE the carousel, always visible below it, rather than being swiped away
     with the meter. Reason: lyrics/meter/artist are all similar-height compact
     content: the queue strip is long and variable — forcing it into a per-pane
     carousel would mean either a jarring height jump between panes or clipping a
     scrollable list inside a cramped carousel slide. Kept it stable instead.
   - **Two real bugs caught by testing, not by reading the code:**
     1. The dismiss dot's `pointerup` listener never fired on a real drag — a 40×5px
        target with no `setPointerCapture()` hands `pointerup` to whatever element
        the cursor ends up over once it moves outside that tiny hit area. Fixed by
        capturing the pointer on `pointerdown`, same fix pattern as the plane itself.
     2. Almost reused the top plane's generic swipe helper for the bottom carousel
        as-is — it assumes a resting `translateX(0)`, but the bottom track's resting
        position depends on which of the 3 panes is active (0%/-33.33%/-66.66%).
        Caught before shipping by reasoning through the math, not after a visual
        glitch: wrote a dedicated handler that adds the live drag delta on top of
        the CURRENT pane's own base offset, so the drag-follow tracks correctly
        from any starting pane, not just the default one.
   - Verified with real pointer-drag simulation (not just click-swap logic): song
     actually changes on swipe, short drags below the 60px threshold spring back
     without triggering, clamping holds at both carousel ends (can't swipe past
     lyrics or past artist), dot-click navigation matches swipe navigation, and
     buttons/inputs inside the swipeable areas still work normally (gesture
     detection explicitly skips `event.target.closest('button, input, a')` on
     pointerdown so taps aren't hijacked as drags).
   - Full regression on existing mobile features after the restructure: play,
     save, shuffle, next/next-button (as opposed to swipe), queue rendering, and
     browse-jump all still pass.
